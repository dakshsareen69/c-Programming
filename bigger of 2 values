#include <stdio.h>

int main() {
    
    int a,b;
    printf("type the numbers");
    scanf ("%d%d", &a,&b);
    
    if (a>b)
    {
        printf("a is greater and b is smaller");
    }
    
    if (a<b)
    {
        printf("b is greater and a is smaller");
    }
    return 0;
}
